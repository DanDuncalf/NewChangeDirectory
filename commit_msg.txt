Linux-first CLI switches and WSL test suite upgrade

CLI Parser (src/cli.c):
- Add space-separated value support for parameterized flags (-conf path,
  --agent check, -d path, etc.)
- Fix Windows /r., /t5, /g-, /x- attached-value tokenization (TOK_SHORT_VAL
  instead of unknown TOK_LONG)
- Fix Linux -r., -t5, -g-, -x- attached-value tokenization for dash-prefixed
  args
- Add known long option detection to prevent misidentifying --agent, -conf, etc.
- Fix agent subcommand token consumption to prevent --json, --limit from
  leaking into main stream
- Add special handling for -g- / -x- removal syntax (consume next token as
  name/pattern)
- Remove unused synth_strdup to eliminate compiler warnings

WSL Test Scripts:
- Replace Windows / prefix with Linux - prefix across all WSL tests
- /agent -> --agent, /r. -> -r ., /t5 -> -t 5, /d -> -d, /g -> -g, /x -> -x
- Fix test_service_wsl.sh log grep to use case-sensitive ERROR (eliminates
  false negatives from L2 info messages)

Build:
- Verified clean builds on both Windows (MSVC) and WSL (GCC)
